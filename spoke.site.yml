# Drush alias to AWS

## DDEV
#  drush.yml
  # drush:
  #   php:
  #     minimum-version: 8.1.0 (or what you need)
  #   paths:
  #     alias-path:
  #       - '/var/www/html/web/.ddev/drush/sites' (container path)

#LIVE
live:
  host: [IP_ADDRESS | URI]
  user: bitnami
  root: /home/bitnami/stack/drupal
  uri: http://[IP_ADDRESS | URI]
  paths:
    drush-script: /opt/bitnami/drupal/vendor/bin/drush
  ssh:
    options: '-i [PATH TO .pem FILE]'

#DEV - coming
#STAGING - coming